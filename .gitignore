# Logs
logs
*.log
npm-debug.log*
yarn-debug.log*
yarn-error.log*
pnpm-debug.log*
lerna-debug.log*

# Dependencies
node_modules

# Build output
dist
dist-ssr
.vite
*.local

# TypeScript incremental build info (generated by `tsc -b`)
*.tsbuildinfo

# Environment files — never commit real values
.env
.env.*
!.env.example

# Deployment
.vercel

# Editor directories and files
.vscode/*
!.vscode/extensions.json
.idea
.DS_Store
*.suo
*.ntvs*
*.njsproj
*.sln
*.sw?
.npmrc

# Local scratch
backup/

# Uncompressed source model. `public/models/character.enc` is the artifact that
# ships; the raw .glb stays local and is re-encrypted via public/models/encrypt.cjs.
public/models/character.glb
